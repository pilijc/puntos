/**
 * Shared layout constants for the Super Admin web dashboard.
 * Used across pages (Dashboard, Users, Stores, etc.) to keep
 * sidebar-relative spacing consistent.
 */

/** Horizontal padding for page-level content (titles, labels) on web */
export const WEB_PAGE_PADDING = 50;

/** Horizontal padding for centered card containers on web */
export const WEB_CARD_PADDING = 24;

/** Max width for centered card containers on web */
export const WEB_CARD_MAX_WIDTH = 1000;
